# ColorLayerSplitter
ColorLayerSplitter — Photoshop (UXP) plugin that splits a layer's colors into separate, auto-named raster layers, non-destructively. For screen printing, flat-color illustration and file prep.
